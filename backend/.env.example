# Application tokens and JWT
APP_TOKEN_PREFIX=user_
APP_JWT_SECRET=dev_super_secret_change_later

# Broker connection
HIVEMQ_HOST=localhost
HIVEMQ_PORT=1883
HIVEMQ_USERNAME=local_backend
HIVEMQ_PASSWORD=local_backend_password
MQTT_CREDENTIALS_TTL=86400
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database backend
DB_BACKEND=sqlite
DATABASE_URL=sqlite:///./app.db

# OAuth (for future auth integration)
OAUTH_CLIENT_ID=replace_me
OAUTH_CLIENT_SECRET=replace_me
OAUTH_REDIRECT_URI=http://localhost:8000/auth/callback
